<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:orientation="vertical" >

    <WebView
        android:id="@+id/webViewResults"
        android:layout_width="250dp"
        android:layout_height="fill_parent"
        android:layout_alignParentTop="true"
        android:layout_margin="10dp" />

    <ProgressBar
        android:id="@+id/ResultsprogressBar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_below="@+id/SpImage"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:indeterminate="true" />

    <TextView
        android:id="@+id/txtResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
         android:layout_margin="10dp"
        android:layout_toRightOf="@+id/webViewResults"
        android:text="Internet connection is required to view results !" />

    <Button
        android:id="@+id/ResultRefresh"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/ResultsprogressBar"
        android:layout_marginRight="23dp"
        android:layout_marginTop="28dp"
        android:text="Refresh" />

</RelativeLayout>
